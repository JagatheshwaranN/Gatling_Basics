To launch the Gatling Recorder

Create a Maven Run Configuration and set the run configuration as gatling:recorder

To execute the Gatling Simulation

Create a Maven Run Configuration and set the run configuration as gatling:test